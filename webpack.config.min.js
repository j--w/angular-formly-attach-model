var webpack = require("webpack");
module.exports = {
  entry: "./src/index.js",
  devtool: "source-map",
  output: {
    path: __dirname,
    filename: "./dist/angular-formly-attach-model.min.js",
    libraryTarget: "umd"
  },
  module: {
    loaders: [{
      test: /\.js$/,
      loader: 'babel?optional[]=runtime',
      exclude: /node_modules/
    }]
  },
  externals: [
    'angular',
    'angular-formly'
  ],
  plugins: [
    new webpack.optimize.UglifyJsPlugin({minimize: true})
  ]
};
